export const MODEL_CONFIG = {

  intent: {
    primary: "groq",
    fallback: "openrouter"
  },

  schema: {
    primary: "gemini",
    fallback: "openrouter"
  },

  appspec: {
    primary: "groq",
    fallback: "openrouter"
  }
};